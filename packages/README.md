# Historical starter packages

These packages are retained from the supplied snapshot. They are not members of the active npm workspace or runtime dependencies. Shared active Orgo UI primitives are composed in apps/web/src/orgo.
