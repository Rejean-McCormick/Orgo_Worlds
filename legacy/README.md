# Historical snapshot source

Original API and web source retained as reference, excluded from active compilation and deployment. The root README and IMPLEMENTATION_STATUS.md identify the new runtime. Do not import legacy services into active modules.
